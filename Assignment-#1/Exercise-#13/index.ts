const my_vehicles: string[] = [ "Heavy Bike", "Bugati", "Vigo V8"];
for (const vehicle of my_vehicles) {
  console.log(`I would like to have a ${vehicle}`);
  console.log(`But ${vehicle} is very expensive`);
  console.log(`${vehicle} will be in my garage inshallah.`);
}
